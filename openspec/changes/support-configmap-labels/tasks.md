# Tasks

- [x] add shared and topic-level `configMapLabels` fields to the admin spec model and validation layer
- [x] merge effective ConfigMap labels during plan building while protecting built-in identity labels
- [x] render effective labels into generated `ConfigMap.metadata.labels`
- [x] add regression coverage for decode, validation, rendering, example parity, and REST plan/validation paths
- [x] update README, architecture notes, understanding docs, checked-in example specs, and changelog
