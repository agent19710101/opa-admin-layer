# Tasks

- [x] define the shared/topic `externalTrafficPolicy` contract and keep the slice limited to `Cluster`/`Local` validation plus effective Service-type compatibility
- [x] add shared and topic-level spec fields, normalization, and validation for `externalTrafficPolicy`
- [x] render inherited/topic-overridden `externalTrafficPolicy` into Service manifests
- [x] add regression coverage across admin, CLI, and REST paths
- [x] update README, architecture notes, understanding docs, changelog, and example specs
