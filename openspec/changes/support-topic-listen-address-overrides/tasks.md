# Tasks

- [x] define topic listen-address override behavior
- [x] add topic-level `listenAddress` to the spec and validation path
- [x] render Deployments and Services from each topic's effective listen address
- [x] add regression coverage across validation, plan rendering, HTTP API, and CLI `render -outdir`
- [x] update README, architecture notes, understanding docs, and changelog
