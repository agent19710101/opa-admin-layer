# Tasks

- [x] define contributor refs for shared `ServiceAccount` plan entries
- [x] include deterministic sorted tenant/topic refs in CLI and REST plan output
- [x] add regression coverage for plan rendering, HTTP API, and exported `plan.json`
- [x] update README, architecture notes, understanding docs, and changelog
