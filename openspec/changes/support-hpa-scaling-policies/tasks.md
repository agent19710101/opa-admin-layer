# Tasks

- [x] record the HPA scaling-policy slice in OpenSpec with proposal, design, and task tracking
- [x] extend the autoscaling spec model, validation, and HPA rendering with `selectPolicy` plus explicit scaling `policies`
- [x] add regression coverage for decoding, admin validation/rendering, and REST validation/plan flows
- [x] update README, architecture notes, understanding docs, examples, changelog, and runtime status after implementation lands
