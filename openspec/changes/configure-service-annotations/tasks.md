# Tasks

- [x] document the missing Service-annotation control and constrain the first slice to Service metadata only
- [ ] add `controlPlane.serviceAnnotations` with normalization and validation
- [ ] render configured annotations into generated Service manifests
- [ ] add regression tests for omitted, configured, and invalid annotation cases
- [ ] update README and understanding docs with the new Service metadata behavior
