# Tasks

- [x] document the missing Service-annotation control and constrain the first slice to Service metadata only
- [x] add `controlPlane.serviceAnnotations` with validation on annotation keys
- [x] render configured annotations into generated Service manifests with YAML-safe quoted values
- [x] add regression tests for omitted, configured, and invalid annotation cases
- [x] update README and understanding docs with the new Service metadata behavior
