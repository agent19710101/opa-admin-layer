# Tasks

- [x] document the inherited ConfigMap metadata gap and constrain the slice to topic overrides on `ConfigMap.metadata.annotations`
- [x] add topic-level `configMapAnnotations` to the spec model and validation layer
- [x] merge topic ConfigMap annotations over shared defaults during plan rendering
- [x] add regression coverage for decode, validation, example-spec parity, and HTTP plan/validation paths
- [x] update README, architecture notes, understanding docs, changelog, and checked-in example specs to describe the inherited ConfigMap annotation behavior
