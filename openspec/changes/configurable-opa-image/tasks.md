# Tasks

- [x] add optional `controlPlane.opaImage` to the spec model with a pinned default
- [x] render the configured image in deployment manifests
- [x] add tests for defaulted and explicit image values
- [x] update docs and example spec to show the new field
