# Tasks

- [x] document the fixed-Service-type gap and the selected allowlist
- [x] add `controlPlane.serviceType` with defaulting and validation
- [x] render the configured Service type into generated manifests
- [x] add regression tests for default, configured, and invalid service-type cases
- [x] update README, understanding docs, and example spec to show the new behavior
