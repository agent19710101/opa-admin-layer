# Tasks

- [x] define the shared owned `ServiceAccount` contract for repeated effective `serviceAccountName` values
- [x] document exact compatibility rules for effective shared service-account annotations and labels
- [x] update validation to reject incompatible shared service-account metadata across topics
- [x] render one shared `ServiceAccount` manifest and `serviceaccount.yaml` artifact per compatible repeated effective name
- [x] add regression coverage for validation, plan rendering, HTTP API, and CLI `render -outdir`
- [x] update README, architecture notes, understanding docs, and changelog
