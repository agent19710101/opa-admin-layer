# Tasks

- [x] define the shared/topic `sessionAffinity` contract and keep the first slice limited to `None` and `ClientIP`
- [x] add shared and topic-level spec fields, normalization, and validation for `sessionAffinity`
- [x] render inherited/topic-overridden `sessionAffinity` into Service manifests
- [x] cover CLI/REST/admin tests plus docs/example spec updates
