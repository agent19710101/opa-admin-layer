# Tasks

- [x] document the deployment health-probe gap and the listen-address-to-port design
- [x] render `containerPort` from the normalized listen address in generated Deployment manifests
- [x] add default readiness and liveness probes to the generated OPA container
- [x] add regression tests for default and explicit listen-address render output
- [x] update README and example output expectations to describe the new manifest behavior
