# Tasks

- [x] record the REST media-type contract as the selected next improvement
- [x] validate supported `Content-Type` values in the HTTP handler while keeping empty `Content-Type` accepted
- [x] add regression tests for supported JSON/YAML types and unsupported media-type failures
- [x] update README and architecture notes to document accepted REST payload media types
