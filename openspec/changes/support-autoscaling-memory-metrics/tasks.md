# Tasks

- [x] document the CPU-only autoscaling limitation and record the memory-metric extension in OpenSpec
- [x] extend the autoscaling spec model, validation, and inherited resource-request checks for memory utilization targets
- [x] render memory resource metrics in generated HPA manifests while keeping deterministic metric ordering
- [x] add regression coverage for validation, plan rendering, and REST behavior
- [x] update README, architecture notes, understanding docs, and changelog to describe autoscaling memory metrics
