# Tasks

- [x] define the narrow rendered-ServiceAccount slice and document why RBAC remains out of scope
- [x] add topic-plan and export support for `ServiceAccount` YAML when an effective inherited `serviceAccountName` is set
- [x] add regression coverage for rendered and omitted ServiceAccount artifacts in plan and CLI export paths
- [x] update README, architecture notes, understanding docs, changelog, and runtime status after implementation lands
