# Tasks

- [x] emit a ConfigMap manifest for each rendered tenant/topic OPA config
- [x] mount the rendered ConfigMap from the generated Deployment at `/config`
- [x] export `configmap.yaml` in the plan tree and document the new artifact
- [x] add regression tests for plan rendering and file export
