# Tasks

- [x] define the ServiceAccount-annotation slice and record why labels/RBAC remain out of scope
- [x] add shared/topic `serviceAccountAnnotations` plus topic removal support to the spec model, validation, and plan rendering
- [x] add regression coverage for decode, validation, plan rendering, CLI export, and REST plan/validation behavior
- [x] update README, architecture notes, understanding docs, examples, changelog, and runtime status after implementation lands
