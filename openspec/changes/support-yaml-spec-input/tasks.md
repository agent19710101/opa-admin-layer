# Tasks

- [x] record YAML spec input as the selected ingestion improvement and architecture follow-up
- [x] add shared strict YAML decoding alongside strict JSON decoding
- [x] accept YAML input through CLI and REST paths
- [x] add regression tests for YAML success and unknown-field failures
- [x] check in `deploy/examples/dev-spec.yaml` so the supported YAML path is runnable from the repository
- [x] update README and understanding notes to document supported YAML ingestion
