# Tasks

- [x] document the per-topic resource-override need and merge semantics
- [x] add topic-level `opaResources` to the spec model and validation layer
- [x] merge topic-level resource fields over shared control-plane defaults during Deployment rendering
- [x] add regression coverage for merged rendering and invalid topic-level resource configuration
- [x] update README, example specs, and understanding docs to describe the override behavior
