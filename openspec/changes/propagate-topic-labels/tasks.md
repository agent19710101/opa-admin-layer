# Tasks

- [x] document the label-propagation gap and selected design
- [x] merge topic labels into generated ConfigMap and Deployment metadata while preserving built-in labels
- [x] add deployment pod-template label propagation for operator tooling consistency
- [x] add regression tests for propagated labels and built-in label precedence
- [x] update the example spec and docs to demonstrate topic labels flowing into rendered manifests
