# Tasks

- [x] document the ConfigMap metadata gap and constrain the first slice to shared ConfigMap object annotations only
- [x] add `controlPlane.configMapAnnotations` to the spec model and validation layer
- [x] render shared ConfigMap annotations into generated ConfigMap manifests
- [x] add regression coverage for decode, validation, CLI render, and HTTP plan/validation paths
- [x] update README, architecture notes, understanding docs, and example specs to describe the new ConfigMap metadata behavior
