# Tasks

- [x] document the missing Kubernetes Service artifact and selected rendering approach
- [x] render a Service manifest per tenant/topic using the derived OPA listen port
- [x] export `service.yaml` in the plan tree and plan JSON
- [x] add regression tests and docs for the new artifact
