# Tasks

- [x] define the shared-binding contract for repeated effective `serviceAccountName` values
- [x] allow repeated effective names during validation
- [x] suppress rendered `ServiceAccount` manifests and `serviceaccount.yaml` artifacts for shared names used by multiple topics
- [x] add regression coverage for validation, plan rendering, HTTP API, and CLI `render -outdir`
- [x] update README, architecture notes, understanding docs, and changelog
